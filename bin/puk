#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
PROJECT_ROOT=$(cd "${SCRIPT_DIR}/.." && pwd)

if command -v uv >/dev/null 2>&1; then
  exec uv run --project "${PROJECT_ROOT}" python -m puk "$@"
fi

exec python3 -m puk "$@"
